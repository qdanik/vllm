# Common dependencies
-r common.txt

numba == 0.60.0; python_version == '3.9' # v0.61 doesn't support Python 3.9. Required for N-gram speculative decoding
numba == 0.61.2; python_version > '3.9'

# Dependencies for NVIDIA GPUs
ray[cgraph]>=2.43.0, !=2.44.* # Ray Compiled Graph, required for pipeline parallelism in V1.
torch==2.7.0
torchaudio==2.7.0
# These must be updated alongside torch
torchvision==0.22.0 # Required for phi3v processor. See https://github.com/pytorch/vision?tab=readme-ov-file#installation for corresponding version
# FlashAttention-3 for H100 Hopper architecture (compute_capability 9.0)
# FA3 provides 1.5-2x speedup over xformers FA2 on H100
flash-attn>=2.7.3,<3.0.0; platform_system == 'Linux' and platform_machine == 'x86_64'  # FA3 support, requires PyTorch >= 2.7

# Optional: xformers as fallback for non-Hopper GPUs
# xformers==0.0.30; platform_system == 'Linux' and platform_machine == 'x86_64'
